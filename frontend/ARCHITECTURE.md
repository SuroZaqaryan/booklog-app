# 📱 BookLog Frontend

Полноценный frontend для приложения BookLog, выполненный в ретро-стиле с использованием RetroUI.

## 🏗️ Архитектура

Проект построен по принципу чистой и масштабируемой архитектуры:

```
src/
├── components/          # UI компоненты
│   ├── retroui/        # RetroUI компоненты (установлены через shadcn)
│   ├── BookCard.tsx    # Карточка книги
│   ├── BookList.tsx    # Список книг
│   ├── AddBookForm.tsx # Форма добавления книги
│   ├── AddBookDialog.tsx # Модальное окно добавления
│   ├── Header.tsx      # Заголовок приложения
│   ├── LoadingSpinner.tsx # Индикатор загрузки
│   └── ErrorMessage.tsx   # Сообщение об ошибке
├── pages/              # Страницы
│   └── HomePage.tsx    # Главная страница
├── services/           # Бизнес-логика и API
│   └── api/
│       ├── axios.ts         # Настройка axios клиента
│       ├── bookService.ts   # Сервис для работы с книгами
│       └── index.ts         # Экспорт сервисов
├── hooks/              # React хуки
│   └── useBooks.ts     # Хук для работы с книгами
├── types/              # TypeScript типы
│   └── book.ts         # Типы книг
├── lib/                # Утилиты
│   └── utils.ts        # Вспомогательные функции
├── App.tsx             # Главный компонент
├── main.tsx            # Точка входа
└── index.css           # Глобальные стили
```

## 🎨 Технологии

- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Axios** - HTTP клиент
- **RetroUI** - UI компоненты в ретро-стиле
- **Tailwind CSS 4** - стилизация
- **Lucide React** - иконки

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Сборка для продакшена

```bash
npm run build
```

## 📡 API Integration

Frontend взаимодействует с backend API через сервисный слой (`services/api`).

### Конфигурация

API endpoint настраивается в `src/services/api/axios.ts`:

```typescript
const API_BASE_URL = 'http://localhost:8000/api/v1';
```

### Доступные методы

Все методы API инкапсулированы в `bookService`:

- `getAllBooks()` - получить все книги
- `createBook(book)` - создать книгу
- `deleteBook(bookId)` - удалить книгу
- `getGenres()` - получить список жанров
- `getStatuses()` - получить список статусов

## 🧩 Компоненты

### Основные компоненты

#### `HomePage`
Главная страница приложения с библиотекой книг.

#### `BookCard`
Карточка отдельной книги с информацией и кнопкой удаления.

#### `BookList`
Отображает список книг в виде сетки карточек.

#### `AddBookDialog`
Модальное окно с формой для добавления новой книги.

#### `AddBookForm`
Форма добавления книги с валидацией и поддержкой кастомных жанров.

### RetroUI компоненты

Используются следующие компоненты RetroUI:
- `Button` - кнопки
- `Card` - карточки
- `Input` - поля ввода
- `Select` - выпадающие списки
- `Dialog` - модальные окна
- `Label` - метки для полей
- `Text` - типографика

## 🪝 Hooks

### `useBooks`

Главный хук для работы с книгами. Предоставляет:

```typescript
{
  books: Book[];           // список книг
  genres: string[];        // список жанров
  loading: boolean;        // состояние загрузки
  error: string | null;    // ошибка загрузки
  addBook: (book) => void; // добавить книгу
  deleteBook: (id) => void; // удалить книгу
  refetch: () => void;     // перезагрузить данные
}
```

## 📝 Типы данных

### `Book`
```typescript
interface Book {
  id: number;
  name: string;
  genre: string | null;
  author: string | null;
}
```

### `BookCreate`
```typescript
interface BookCreate {
  name: string;
  genre?: string | null;
  author?: string | null;
}
```

## 🎨 Дизайн

Приложение использует ретро-дизайн с помощью RetroUI:

- **Цветовая схема**: желтый (#ffdb33) как primary цвет
- **Шрифты**: 
  - Заголовки: Archivo Black
  - Текст: Space Grotesk
- **Тени**: жесткие тени (hard shadows) для ретро-эффекта
- **Границы**: резкие углы без скругления

## 🔄 Состояние приложения

Состояние управляется через React hooks:
- Локальное состояние компонентов (`useState`)
- Кастомный хук `useBooks` для работы с данными

## 🛡️ Обработка ошибок

- **Сетевые ошибки**: перехватываются axios interceptor
- **Ошибки API**: обрабатываются в сервисном слое
- **UI ошибки**: отображаются через `ErrorMessage` компонент
- **Ошибки действий**: показываются в уведомлениях

## 🚦 Состояния загрузки

Приложение корректно обрабатывает различные состояния:
- **Загрузка**: показывается `LoadingSpinner`
- **Ошибка**: показывается `ErrorMessage` с возможностью повтора
- **Пустой список**: показывается приглашение добавить первую книгу
- **Успех**: отображается список книг

## 📦 Структура данных

### Жанры
Жанры загружаются с backend и могут быть выбраны из списка или введены вручную.

### Книги
Книги хранятся в состоянии компонента и синхронизируются с backend при каждом действии.

## 🎯 Особенности реализации

1. **Переиспользуемые компоненты**: все UI компоненты максимально переиспользуемы
2. **Типобезопасность**: полная типизация с TypeScript
3. **Чистая архитектура**: четкое разделение на слои (UI, services, hooks)
4. **Оптимизация**: минимальное количество перерендеров
5. **UX**: информативные сообщения об ошибках и состояниях загрузки

## 🔮 Возможные улучшения

- [ ] Добавить поиск и фильтрацию книг
- [ ] Реализовать редактирование книг
- [ ] Добавить пагинацию для больших списков
- [ ] Реализовать сортировку книг
- [ ] Добавить возможность отмечать статус книги (прочитана, в процессе и т.д.)
- [ ] Реализовать dark mode toggle
- [ ] Добавить анимации переходов
- [ ] Реализовать offline режим с кешированием

## 📄 Лицензия

Проект создан для учебных целей.

