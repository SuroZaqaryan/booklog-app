# Краткое руководство - Загрузка изображений книг

## ✅ Что реализовано

Приложение BookLog теперь поддерживает загрузку и отображение обложек книг!

## 🎯 Основные изменения

### Backend (Python/FastAPI)
- POST `/api/v1/book` теперь принимает файл изображения
- GET `/api/v1/book` возвращает URL изображения в поле `image_url`
- Изображения сохраняются в директории `backend/uploads/images/`
- Изображения доступны по URL: `http://localhost:8000/uploads/images/{filename}`

### Frontend (React/TypeScript)
- Форма добавления книги теперь имеет поле для загрузки изображения
- Показывается превью изображения перед отправкой
- Карточки книг отображают обложки

## 🚀 Быстрый старт

### 1. Применить миграцию базы данных
```bash
cd backend
uv run alembic upgrade head
```

### 2. Установить новую зависимость (уже сделано)
```bash
cd backend
uv sync
```

### 3. Запустить backend
```bash
cd backend
uv run uvicorn app.main:app --reload
```

### 4. Запустить frontend
```bash
cd frontend
npm run dev
```

## 📸 Как использовать

1. Откройте приложение в браузере: http://localhost:5173
2. Нажмите "Добавить книгу"
3. Заполните название книги
4. Нажмите на поле "Обложка книги" и выберите изображение
5. Появится превью выбранного изображения
6. Нажмите "Добавить книгу"
7. Обложка появится в карточке книги!

## 🔧 Технические детали

### Формат запроса
- **Раньше:** `Content-Type: application/json`
- **Сейчас:** `Content-Type: multipart/form-data`

### Параметры POST запроса
```
name: string (обязательный)
genre: string (опциональный)
author: string (опциональный)
image: file (опциональный)
```

### Ограничения
- Форматы: JPG, PNG, GIF, WEBP
- Максимальный размер: 5 MB

### Пример ответа GET
```json
{
  "id": 1,
  "name": "Война и мир",
  "genre": "Классика",
  "author": "Лев Толстой",
  "image_url": "uploads/images/abc123.jpg"
}
```

## 📂 Измененные файлы

### Backend
- `app/api/v1/endpoints/books.py` - endpoint теперь принимает файлы
- `app/models/book.py` - добавлено поле image_url
- `app/schemas/book.py` - добавлено поле image_url в схемы
- `app/services/book.py` - обновлен метод создания книги
- `app/main.py` - добавлено монтирование статических файлов
- `app/utils/image.py` - новый модуль для работы с изображениями
- `pyproject.toml` - добавлен python-multipart

### Frontend
- `src/types/book.ts` - добавлено поле image_url
- `src/services/api/bookService.ts` - используется FormData
- `src/components/AddBookForm.tsx` - добавлено поле загрузки изображения
- `src/components/BookCard.tsx` - отображение обложки

## 🧪 Тестирование

### Вариант 1: Через приложение
Используйте интерфейс как описано выше

### Вариант 2: Через Swagger UI
1. Откройте http://localhost:8000/api/docs
2. Найдите POST `/api/v1/book`
3. Нажмите "Try it out"
4. Заполните поля и загрузите файл

### Вариант 3: Через curl
```bash
curl -X POST "http://localhost:8000/api/v1/book" \
  -H "accept: application/json" \
  -F "name=Тестовая книга" \
  -F "author=Тестовый автор" \
  -F "genre=Тестовый жанр" \
  -F "image=@/path/to/image.jpg"
```

## ❓ Возможные проблемы

### Ошибка: "alembic command not found"
```bash
cd backend
uv run alembic upgrade head
```

### Ошибка: "No module named 'multipart'"
```bash
cd backend
uv sync
```

### Изображения не отображаются
- Проверьте, что backend запущен
- Проверьте консоль браузера на наличие ошибок CORS
- Убедитесь, что URL изображения правильный

## 📖 Подробная документация

Смотрите файл `IMAGE_UPLOAD_GUIDE.md` для детальной информации об архитектуре и реализации.

## ✨ Готово!

Теперь ваше приложение BookLog поддерживает загрузку и отображение обложек книг! 🎉
