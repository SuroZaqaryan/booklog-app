# 📚 BookLog Backend

Backend API для приложения управления книгами на FastAPI с модульной архитектурой.

## 🏗️ Архитектура v2.0

Проект построен по принципу **модульной архитектуры** (vertical slice architecture):

```
src/
├── books/          # Модуль книг - всё о книгах в одном месте
├── genres/         # Модуль жанров
├── core/           # Ядро приложения (config, database)
└── common/         # Общие утилиты (enums, image utils)
```

**Преимущества:**
- ✅ Высокая когезия - все файлы одной фичи в одном месте
- ✅ Легко масштабировать - просто добавляй новые модули
- ✅ Удобно тестировать - каждый модуль независим
- ✅ Меньше конфликтов в git
- ✅ Легко выделить в микросервис

Подробнее см. [ARCHITECTURE.md](ARCHITECTURE.md)

## 🚀 Быстрый старт

### Установка зависимостей

```bash
uv sync
```

### Запуск сервера

```bash
# Вариант 1: через uvicorn напрямую
uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

# Вариант 2: через launcher
uv run python main.py
```

### Миграции БД

```bash
# Применить все миграции (первый запуск)
python -m alembic upgrade head

# Создать новую миграцию (после изменения моделей)
python -m alembic revision --autogenerate -m "описание изменений"

# Проверить текущую версию БД
python -m alembic current

# Посмотреть историю миграций
python -m alembic history
```

**⚠️ Важно:** После изменения ORM моделей нужно создать и применить миграцию!

📖 **Подробное руководство:** [ALEMBIC_GUIDE.md](ALEMBIC_GUIDE.md)

## 📖 Документация API

После запуска сервера доступна по адресам:
- **Swagger UI**: http://localhost:8000/api/docs
- **ReDoc**: http://localhost:8000/api/redoc
- **OpenAPI JSON**: http://localhost:8000/api/v1/openapi.json

## 🔗 Endpoints

### Books (`/api/v1/book`)
- `GET /api/v1/book` - получить все книги
- `POST /api/v1/book` - создать книгу (с поддержкой загрузки изображений)
- `DELETE /api/v1/book/{id}` - удалить книгу
- `GET /api/v1/book/genres` - получить список жанров из БД
- `GET /api/v1/book/statuses` - получить список статусов

## 📁 Структура проекта

```
backend/
├── src/                       # Исходный код (новая модульная структура)
│   ├── books/                # Модуль книг
│   │   ├── models.py        # ORM модели
│   │   ├── schemas.py       # Pydantic схемы (DTO)
│   │   ├── repository.py    # Работа с БД
│   │   ├── service.py       # Бизнес-логика
│   │   └── router.py        # API endpoints
│   ├── genres/              # Модуль жанров
│   │   └── models.py
│   ├── core/                # Ядро приложения
│   │   ├── config.py       # Настройки
│   │   ├── database.py     # Подключение к БД
│   │   └── base.py         # Base для ORM
│   ├── common/             # Общие компоненты
│   │   ├── enums.py       # Enum'ы
│   │   └── utils/
│   │       └── image.py   # Утилиты изображений
│   └── main.py            # Точка входа FastAPI
│
├── alembic/               # Миграции БД
│   └── versions/
│
├── uploads/              # Загруженные файлы
│   └── images/
│
├── main.py              # Launcher (uvicorn)
├── pyproject.toml      # Зависимости (uv)
├── alembic.ini        # Конфигурация Alembic
└── books.db          # SQLite база данных
```

## 🛠️ Технологии

- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **Alembic** - миграции базы данных
- **Pydantic** - валидация данных
- **aiosqlite** - асинхронный драйвер SQLite
- **uv** - быстрый менеджер пакетов Python

## 🎯 Добавление нового модуля

Пример: добавить модуль `authors`

1. Создать структуру:
```bash
mkdir src/authors
```

2. Создать файлы:
```
src/authors/
├── __init__.py
├── models.py       # ORM модели
├── schemas.py      # Pydantic схемы
├── repository.py   # Работа с БД
├── service.py      # Бизнес-логика
└── router.py       # API endpoints
```

3. Подключить в `src/main.py`:
```python
from src.authors.router import router as authors_router

app.include_router(
    authors_router,
    prefix="/api/v1/authors",
    tags=["authors"]
)
```

4. Создать и применить миграцию:
```bash
alembic revision --autogenerate -m "Add authors table"
alembic upgrade head
```

## 🧪 Тестирование

```bash
# Запустить тесты
pytest

# С покрытием
pytest --cov=src
```

Структура тестов:
```
tests/
├── books/
│   ├── test_repository.py
│   ├── test_service.py
│   └── test_router.py
└── genres/
```

## 📊 База данных

- **SQLite** для разработки (books.db)
- Поддержка миграций через Alembic
- Асинхронные запросы через aiosqlite

### Модели
- `books` - книги (id, name, genre, author, image_url)
- `genres` - жанры (id, name)

## 🖼️ Загрузка изображений

- Поддержка форматов: JPG, PNG, GIF, WebP
- Максимальный размер: 5MB
- Хранение: `uploads/images/`
- URL доступ: `/uploads/images/{filename}`

## 📚 Дополнительные документы

- [ARCHITECTURE.md](ARCHITECTURE.md) - подробное описание архитектуры
- [ALEMBIC_GUIDE.md](ALEMBIC_GUIDE.md) - полное руководство по миграциям
- [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md) - руководство по рефакторингу
- [REFACTORING_SUMMARY.md](REFACTORING_SUMMARY.md) - итоги рефакторинга

## 🐛 Troubleshooting

### Проблема: Импорты не работают
```bash
# Убедитесь что запускаете из корня backend/
cd backend
uv run uvicorn src.main:app --reload
```

### Проблема: Миграции не применяются
```bash
# Проверьте текущую версию
python -m alembic current

# Примените миграции
python -m alembic upgrade head
```

### Проблема: База данных заблокирована
```bash
# Остановите все запущенные процессы сервера
# Затем перезапустите
```

## 🔧 Настройки окружения

Создайте файл `.env` (опционально):
```env
DATABASE_URL=sqlite+aiosqlite:///./books.db
PROJECT_NAME=Books Manager
```

## 🌟 Особенности

- ✅ Асинхронная архитектура (async/await)
- ✅ Автоматическая валидация через Pydantic
- ✅ Автоматическая документация API (OpenAPI)
- ✅ CORS настроен для разработки
- ✅ Обработка ошибок и валидация файлов
- ✅ Модульная структура для масштабирования
- ✅ Type hints везде для лучшей читаемости

## 👨‍💻 Разработка

### Code Style
- Следуем PEP 8
- Type hints обязательны
- Docstrings для всех публичных функций

### Git Workflow
```bash
# Создать feature branch
git checkout -b feature/new-module

# После изменений
git add .
git commit -m "Add new module"
git push
```

## 📦 Зависимости

Основные:
- fastapi
- sqlalchemy[asyncio]
- aiosqlite
- alembic
- pydantic-settings
- python-multipart (для загрузки файлов)
- uvicorn[standard]

Dev:
- pytest
- pytest-cov
- httpx (для тестирования)

## 🚀 Production

Для продакшна рекомендуется:
1. Использовать PostgreSQL вместо SQLite
2. Настроить переменные окружения
3. Использовать gunicorn + uvicorn workers
4. Настроить логирование
5. Добавить мониторинг

---

**Версия:** 2.0.0  
**Автор:** BookLog Team  
**Лицензия:** MIT
