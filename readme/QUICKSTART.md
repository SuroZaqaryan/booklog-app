# 🚀 Инструкция по запуску BookLog

## Запуск Backend

1. Перейдите в директорию backend:
```bash
cd /home/suren/Документы/repos/python/booklog-app/backend
```

2. Запустите сервер:
```bash
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

3. API будет доступно по адресу: http://localhost:8000
4. Swagger документация: http://localhost:8000/api/docs

## Запуск Frontend

1. Перейдите в директорию frontend:
```bash
cd /home/suren/Документы/repos/python/booklog-app/frontend
```

2. Установите зависимости (если еще не установлены):
```bash
npm install
```

3. Запустите dev сервер:
```bash
npm run dev
```

4. Приложение будет доступно по адресу: http://localhost:5173

## Проверка работоспособности

1. Убедитесь, что backend запущен и доступен
2. Откройте frontend в браузере
3. Попробуйте добавить книгу через форму
4. Проверьте отображение книги в списке
5. Попробуйте удалить книгу

## Возможные проблемы

### CORS ошибки
Если возникают CORS ошибки, убедитесь, что в backend настроены правильные CORS заголовки для `http://localhost:5173`

### Ошибка подключения к API
Проверьте, что:
- Backend запущен на порту 8000
- URL в `frontend/src/services/api/axios.ts` соответствует адресу backend
- Нет firewall блокировок

### Компоненты не отображаются
Убедитесь, что все зависимости установлены:
```bash
cd frontend
npm install
```

## Полезные команды

### Frontend
```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для продакшена
npm run preview  # Просмотр production сборки
npm run lint     # Проверка кода
```

### Backend
```bash
# Применить миграции БД
python -m alembic upgrade head

# Проверить статус миграций
python -m alembic current
```

